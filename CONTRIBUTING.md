# 📖 Contributing Guidelines - Руководство для разработчиков

Спасибо за интерес к разработке Scarlet Bot! Это руководство поможет вам начать вносить свой вклад в проект.

## 🚀 Начало работы

### 1. Подготовка окружения

```bash
# Клонируйте форк репозитория
git clone https://github.com/ваш_юзер/Scarlet-Bot.git
cd Scarlet-Bot

# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # или venv\Scripts\activate на Windows

# Установите зависимости
pip install -r requirements.txt

# Создайте .env файл
cp .env.example .env
# Добавьте ваш токен в .env
```

### 2. Создайте новую ветку

```bash
git checkout -b feature/описание-функции
# или
git checkout -b fix/описание-баги
```

## 📋 Требования для Pull Request

### ✅ Перед отправкой PR убедитесь:

1. **Нет токенов в коде**
   ```bash
   grep -r "MTQ0NDAwNzI2" .  # Не должно быть результатов
   ```

2. **Код следует стилю Python (PEP 8)**
   ```bash
   black "BOT 1/main.py"
   isort "BOT 1/main.py"
   ```

3. **Нет синтаксических ошибок**
   ```bash
   flake8 "BOT 1/main.py"
   ```

4. **Хорошее качество кода**
   ```bash
   pylint "BOT 1/main.py"
   ```

5. **Проверка безопасности**
   ```bash
   bandit -r "BOT 1/"
   ```

6. **Добавлены логирования**
   ```python
   logger.info(f'✓ Событие | Описание...')
   logger.error(f'❌ Ошибка | Описание...')
   ```

## 📝 Правила написания кода

### Структура функции команды

```python
@bot.command(name='example', aliases=['ex', 'пример'])
async def example(ctx, arg1: str, arg2: int = 10):
    """Описание команды"""
    user_id = str(ctx.author.id)
    
    # Логирование
    logger.info(f'✓ КОМАНДА | Пользователь: {ctx.author} | Аргумент: {arg1}')
    
    try:
        # Основной код
        result = arg1.upper()
        
        # Логирование результата
        logger.info(f'✓ УСПЕХ | Результат: {result}')
        await ctx.send(f"✅ Результат: {result}")
        
    except Exception as e:
        # Логирование ошибки
        logger.error(f'❌ ОШИБКА | Описание: {str(e)}')
        await ctx.send(f"❌ Ошибка!")
```

### Правила кода

1. **Используйте логирование вместо print()**
   ```python
   # ❌ Плохо
   print("Событие произошло")
   
   # ✅ Хорошо
   logger.info('✓ СОБЫТИЕ | Описание')
   ```

2. **Специфичные исключения вместо bare except**
   ```python
   # ❌ Плохо
   try:
       code()
   except:
       pass
   
   # ✅ Хорошо
   try:
       code()
   except ValueError as e:
       logger.error(f'Ошибка: {e}')
   ```

3. **Используйте type hints**
   ```python
   # ❌ Плохо
   def add_xp(guild_id, user_id, amount):
       pass
   
   # ✅ Хорошо
   def add_xp(guild_id: str, user_id: str, amount: int) -> int:
       pass
   ```

4. **Комментарии для сложной логики**
   ```python
   # ✅ Хорошо
   # Проверяем наличие прав перед выполнением
   if not ctx.author.guild_permissions.administrator:
       await ctx.send("❌ Недостаточно прав!")
       return
   ```

5. **Константы UPPERCASE**
   ```python
   # ✅ Хорошо
   MAX_TIMEOUT_MINUTES = 1440
   DEFAULT_VOICE_XP = 5
   ```

## 🔒 Безопасность

### Никогда не коммитьте:
- ❌ Discord токены
- ❌ API ключи
- ❌ Пароли
- ❌ Приватные данные

### Всегда используйте:
```python
import os
from dotenv import load_dotenv

load_dotenv()
TOKEN = os.getenv('DISCORD_BOT_TOKEN')
```

## 📝 Commit Messages

Используйте понятные сообщения коммитов:

```bash
# ✅ Хорошо
git commit -m "feat: add !ping command with latency display"
git commit -m "fix: resolve music queue bug on bot disconnect"
git commit -m "docs: update README with new features"

# ❌ Плохо
git commit -m "update"
git commit -m "fix stuff"
git commit -m "asdf"
```

## 🧪 Тестирование

Тестируйте вашу функцию локально перед отправкой PR:

```python
# Пример теста в Discord
!ping
!help
!balance @user
```

## 📊 Pull Request Процесс

1. **Создайте PR с описанием:**
   ```markdown
   ## Описание
   Краткое описание того, что вы делаете
   
   ## Тип изменения
   - [ ] Новая функция
   - [ ] Исправление ошибки
   - [ ] Документация
   
   ## Проверки
   - [ ] Нет токенов в коде
   - [ ] Код отформатирован (black, isort)
   - [ ] Нет синтаксических ошибок
   - [ ] Добавлены логирования
   - [ ] Тестировано локально
   ```

2. **Запустите локальные проверки перед push:**
   ```bash
   python code-quality.py      # Проверка качества кода
   python pr-check.py          # Проверка безопасности и токенов
   ```
   Все проверки должны быть зелеными ✅

3. **Создайте Pull Request на GitHub**
   ```markdown
   ## Описание
   Краткое описание изменений
   
   ## Тип изменения
   - [ ] Новая функция
   - [ ] Исправление ошибки
   - [ ] Документация
   
   ## Проверки
   - [ ] Нет токенов в коде
   - [ ] Запущены code-quality.py и pr-check.py
   - [ ] Код отформатирован
   - [ ] Нет синтаксических ошибок
   - [ ] Добавлены логирования
   - [ ] Тестировано локально
   ```

4. **Ревью от мейнтейнеров**
   - Могут попросить изменения
   - Будут даны рекомендации по коду
   - После утверждения ваш код будет объединен в main

## ❓ Вопросы?

Если что-то непонятно:
- Создайте Issue с вопросом
- Посмотрите существующие Issues и PRs
- Обратитесь к мейнтейнерам

## 🙏 Спасибо!

Ваш вклад помогает сделать Scarlet Bot еще лучше!

---

**Последнее обновление:** 13 декабря 2025 г.
